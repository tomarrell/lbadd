// Package column describes columns inside the database. A column consists of a
// type, type parameters and a few additional attributes, such as if the column
// is nullable, if it is a primary key etc.
package column
