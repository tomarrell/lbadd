// Package schema implements a schema structure. Every method and function
// defined in here works with arguments instead of an SQL statement. A schema
// consists of zero or more tables that can be retrieved.
package schema
